// Create a new string called "myNewString" that holds the value of "Developer", using the value from "myString"
const myString = "developer";

console.log(myString.substring(0, 1).toUpperCase() + myString.slice(1));
